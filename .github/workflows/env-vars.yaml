name: Environment variables
on: [push]

jobs:
  env-vars-context:
    runs-on: ubuntu-latest
    steps:
      - name: Log ENV Var
        run: echo $GITHUB_REF
      - name: Log Context
        run: echo '${{ github.ref }}'